# After you’ve edited some code and want to push the change
git status
git add .
git commit -m "Describe what changed"
git push

# now if you want to sync your local code with the github one online:
cd ~/.../segc_wl
git remote -v # should show fetch and push
git pull origin main

# to make a branch:
git checkout -b my-new-branch
# this creates the branch and switches you onto it
git push -u origin my-new-branch
# the -u makes future git push and git pull default to this branch

git add ../Project.toml
git add -A ../src
git add -A ../examples/4_atoms